{% extends 'base.html.twig' %}

{% block title 'Toutes les annonces' %}

{% block body %}
<div class="container my-4">
   <header class="text-center mb-5">
      <h1 class="display-6">Toutes les annonces</h1>
      <p>Les annonces sont listées dès la dernière</p>
   </header>
   <div class="row mb-5">
      {% for ad in paginator.fetchItems %}
         <div class="col-xl-3 col-md-6 col-sm-12 mb-4">
            {{  include("cards/ad_card.html.twig") }}
         </div>
      {% endfor %}
   </div>

   <!-- Pagination -->
   {{ paginator.render() }}
</div>
{% endblock %}
